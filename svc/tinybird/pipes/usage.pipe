NODE daily_usage
SQL >
    SELECT
        teamId,
        COUNT(*) as checks,
        toYYYYMMDD(checks__v2.time) as day
    FROM checks__v2
    GROUP BY
        checks__v2.teamId,
        toYYYYMMDD(checks__v2.time)


