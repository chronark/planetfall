VERSION 1

NODE filter
SQL >
    %
    SELECT teamId,countMerge(usage) as usage, year, month, day
    FROM daily_usage
    WHERE
        teamId = {{ String(teamId, required=True) }}
        AND year = {{ UInt16(year, required=True) }}
        AND month = {{ UInt8(month, required=True) }}
    GROUP BY teamId, year, month,day


