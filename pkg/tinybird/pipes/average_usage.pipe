VERSION 1
TOKEN "live" READ



NODE average_usage

SQL >

    %
    SELECT COUNT() / {{Int32(days, 7, description="Average over this many days", required=False)}}  as average
    FROM checks__v1
    WHERE time > now() - INTERVAL {{Int32(days, 7, description="Average over this many days", required=False)}} DAY